version = 0.1
[default.deploy.parameters]
stack_name = "{{ stackName }}"
resolve_s3 = true
s3_prefix = "{{ s3Prefix }}"
region = "{{ region }}"
profile = "{{ profile }}"
capabilities = "CAPABILITY_IAM"
image_repositories = []
confirm_changeset = true
